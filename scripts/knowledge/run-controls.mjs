#!/usr/bin/env node
import fs from 'node:fs';import path from 'node:path';import process from 'node:process';import {spawnSync} from 'node:child_process';import {canonicalStringify,digest,fileDigest} from './lib/canonical-json.mjs';
const NOW=process.env.KNOWLEDGE_NOW||'2026-07-31T00:00:00Z';
const streams=[
{id:'WS1',name:'source-independence',lead:'agent:source-lineage-lead',verifier:'agent:source-lineage-independent-verifier',test:'scripts/knowledge/test-source-independence.mjs',outputs:['knowledge/receipts/source-independence','knowledge/candidates/source-independence']},
{id:'WS2',name:'temporal-decay',lead:'agent:temporal-lead',verifier:'agent:temporal-independent-verifier',test:'scripts/knowledge/test-temporal-decay.mjs',outputs:['knowledge/receipts/temporal-decay','knowledge/projections/temporal-decay']},
{id:'WS3',name:'claim-identity',lead:'agent:claim-identity-lead',verifier:'agent:claim-identity-independent-verifier',test:'scripts/knowledge/test-semantic-duplicates.mjs',outputs:['knowledge/receipts/claim-identity','knowledge/candidates/claim-identity']},
{id:'WS4',name:'corrections',lead:'agent:correction-lead',verifier:'agent:correction-independent-verifier',test:'scripts/knowledge/test-corrections.mjs',outputs:['knowledge/receipts/corrections','knowledge/projections/corrections']},
{id:'WS5',name:'sensitive-data',lead:'agent:data-steward',verifier:'agent:data-steward-independent-verifier',test:'scripts/knowledge/test-sensitive-data.mjs',outputs:['knowledge/receipts/sensitive-data','knowledge/quarantine/sensitive-data']},
{id:'WS6',name:'calibration',lead:'agent:calibration-lead',verifier:'agent:calibration-independent-verifier',test:'scripts/knowledge/test-calibration.mjs',outputs:['knowledge/receipts/calibration','knowledge/projections/calibration']},
{id:'WS7',name:'budgets',lead:'agent:budget-governor',verifier:'agent:budget-independent-verifier',test:'scripts/knowledge/test-budgets.mjs',outputs:['knowledge/receipts/budgets']},
{id:'WS8',name:'notebook-boundary',lead:'agent:notebook-boundary-lead',verifier:'agent:notebook-boundary-independent-verifier',test:'scripts/knowledge/test-notebook-export.mjs',outputs:['knowledge/receipts/notebook-boundary','knowledge/quarantine/notebook-exports']}
];
const canonicalDirs=['knowledge/sources','knowledge/entities','knowledge/observations','knowledge/relationships','knowledge/patterns','knowledge/opportunities','knowledge/strategies','knowledge/experiments','knowledge/runs'];
function list(dir){if(!fs.existsSync(dir))return[];const out=[];for(const e of fs.readdirSync(dir,{withFileTypes:true}).sort((a,b)=>a.name.localeCompare(b.name))){const p=path.join(dir,e.name);if(e.isDirectory())out.push(...list(p));else if(e.name.endsWith('.json'))out.push(p.replaceAll('\\','/'));}return out;}
async function treeDigest(dirs){const entries=[];for(const f of dirs.flatMap(list).sort())entries.push({path:f,sha256:await fileDigest(f)});return{entries,digest:digest(entries)};}
function runTest(s){const r=spawnSync(process.execPath,[s.test],{cwd:process.cwd(),encoding:'utf8',env:{...process.env,KNOWLEDGE_NOW:NOW}});const all=`${r.stdout||''}\n${r.stderr||''}`;const matches=[...all.matchAll(/DIGEST\s+[^\s]+\s+([0-9a-f]{64})/g)];return{exitCode:r.status??2,digest:matches.at(-1)?.[1]||null,output:all.trim().split('\n').slice(-12)};}
async function main(){const validationRun=spawnSync(process.execPath,['scripts/knowledge/validate.mjs'],{cwd:process.cwd(),encoding:'utf8',env:{...process.env,KNOWLEDGE_NOW:NOW}});if(validationRun.stdout)process.stdout.write(validationRun.stdout);if(validationRun.stderr)process.stderr.write(validationRun.stderr);const integrationValidationPassed=validationRun.status===0;const canonicalBefore=await treeDigest(canonicalDirs);const workstreams=[];const approvals=[];for(const s of streams){console.log(`INFO ${s.id} lead=${s.lead} verifier=${s.verifier}`);const first=runTest(s),second=runTest(s);const approved=first.exitCode===0&&second.exitCode===0&&first.digest&&first.digest===second.digest;const reason=approved?'Two fresh-process executions passed with identical output digests.':'Execution failed or digests differed.';const approval={approvalId:`verifier:${s.id}:${digest({s:s.id,d:first.digest,approved}).slice(0,12)}`,type:'IndependentVerifierApproval',workstream:s.id,leadAgent:s.lead,verifierAgent:s.verifier,verificationMode:'fresh-process-reexecution-and-adversarial-fixture-review',generatedAt:NOW,status:approved?'approved':'rejected',runDigests:[first.digest,second.digest],exitCodes:[first.exitCode,second.exitCode],reason,canonicalMutationAllowed:false};approvals.push(approval);fs.mkdirSync('knowledge/receipts/verifiers',{recursive:true});fs.writeFileSync(`knowledge/receipts/verifiers/${s.id.toLowerCase()}-verifier-approval.json`,canonicalStringify(approval)+'\n');workstreams.push({id:s.id,name:s.name,leadAgent:s.lead,verifierAgent:s.verifier,status:approved?'complete':'failed',tests:[{command:`node ${s.test}`,run:1,exitCode:first.exitCode,digest:first.digest},{command:`node ${s.test}`,run:2,exitCode:second.exitCode,digest:second.digest}],outputs:s.outputs,knownLimitations:[],verifierApproval:approval.approvalId});console.log(`${approved?'PASS':'FAIL'} ${s.id} ${reason}`);if(!approved){console.log(first.output.join('\n'));console.log(second.output.join('\n'));}}
const canonicalAfter=await treeDigest(canonicalDirs);const governance=JSON.parse(fs.readFileSync('knowledge/config/governance.json','utf8'));const boundaryChecks=[
{id:'integration-schema-compatibility',pass:integrationValidationPassed,detail:`validate exit=${validationRun.status??2}`},
{id:'canonical-json-unchanged',pass:canonicalBefore.digest===canonicalAfter.digest,detail:`before=${canonicalBefore.digest} after=${canonicalAfter.digest}`},
{id:'daily-automation-cannot-accept',pass:governance.automation?.mayAcceptKnowledge===false,detail:String(governance.automation?.mayAcceptKnowledge)},
{id:'daily-automation-genesis-deferred',pass:governance.automation?.dailyGenesisState==='deferred',detail:String(governance.automation?.dailyGenesisState)},
{id:'no-paid-infrastructure',pass:governance.automation?.maySpendMoney===false,detail:String(governance.automation?.maySpendMoney)},
{id:'notebook-canonical-write-denied',pass:governance.notebooks?.canonicalWriteAllowed===false,detail:String(governance.notebooks?.canonicalWriteAllowed)}
];
const controlsPassed=workstreams.every(w=>w.status==='complete')&&boundaryChecks.every(c=>c.pass);const changedPaths=['scripts/knowledge/detect-semantic-duplicates.mjs','scripts/knowledge/test-semantic-duplicates.mjs','scripts/knowledge/validate-corrections.mjs','scripts/knowledge/propagate-invalidation.mjs','scripts/knowledge/test-corrections.mjs','scripts/knowledge/export-notebook-candidates.mjs','scripts/knowledge/test-notebook-export.mjs','scripts/knowledge/run-controls.mjs','scripts/knowledge/build-workbench.mjs','tools/graph-workbench/index.html','knowledge/schema/correction-event.schema.json','knowledge/schema/notebook-export.schema.json','knowledge/fixtures/corrections/**','knowledge/fixtures/notebooks/**','docs/strategy-genesis/OPERATOR_CONTROLS.md','docs/strategy-genesis/ADVERSARIAL_REPORT.md','.github/workflows/knowledge-controls.yml','package.json'];
const core={reportId:'strategy-genesis-completion:ws1-ws10-v0',type:'StrategyGenesisCompletionReport',generatedAt:NOW,branch:'feat/strategy-genesis-engine-v0',executionIssue:24,pullRequest:23,pullRequestMustRemainDraft:true,canonicalStore:'git-versioned-json',workstreams:[...workstreams,{id:'WS9',name:'integration',leadAgent:'agent:integration-lead',verifierAgent:'agent:integration-independent-verifier',status:controlsPassed?'complete':'failed',tests:[{command:'node scripts/knowledge/run-controls.mjs',digest:null}],outputs:['package scripts','CI gates','Graph Workbench control views','stage receipts','operator docs'],knownLimitations:['Graph Workbench remains a disposable static projection; no graph database is authoritative.'],verifierApproval:null},{id:'WS10',name:'adversarial-verification',leadAgent:'agent:adversarial-verifier',verifierAgent:'github-actions:knowledge-controls',status:controlsPassed?'complete':'failed',tests:boundaryChecks,outputs:['knowledge/receipts/adversarial/completion-report.json','docs/strategy-genesis/ADVERSARIAL_REPORT.md'],knownLimitations:['Programmatic verifier approval is independent by process and CI runtime, not a human semantic review.'],verifierApproval:null}],boundaryChecks,canonicalDigestBefore:canonicalBefore.digest,canonicalDigestAfter:canonicalAfter.digest,changedPaths,receipts:approvals.map(a=>`knowledge/receipts/verifiers/${a.workstream.toLowerCase()}-verifier-approval.json`),knownLimitations:['Heuristic duplicate detection is deterministic lexical analysis without embeddings and intentionally favors review queues over recall.','The control proof is offline and fixture-backed; external source availability is not re-fetched.'],remainingRisks:controlsPassed?['Human reviewer must inspect semantic candidates, correction impact queues, and the draft PR before merge.']:['One or more workstream gates failed; inspect verifier receipts.'],mergeRecommendation:controlsPassed?'READY_FOR_HUMAN_REVIEW_KEEP_DRAFT':'DO_NOT_MERGE',allHeuristicsNonMutating:true,knowledgeAccepted:false};const suiteDigest=digest(core);const report={...core,digest:suiteDigest};fs.mkdirSync('knowledge/receipts/adversarial',{recursive:true});fs.writeFileSync('knowledge/receipts/adversarial/completion-report.json',canonicalStringify(report)+'\n');console.log(`DIGEST controls ${suiteDigest}`);if(!controlsPassed)process.exit(1);console.log('PASS WS1-WS10 control gates complete; PR remains draft and requires human review');}
main().catch(e=>{console.error(`FAIL ${e.stack||e.message}`);process.exit(2)});
