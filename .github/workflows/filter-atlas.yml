name: Filter Atlas

on:
  pull_request:
    paths:
      - 'packages/svg-filter-atlas/**'
      - 'src/filter-atlas.ts'
      - 'src/main.ts'
      - 'package.json'
      - 'package-lock.json'
      - '.github/workflows/filter-atlas.yml'
  push:
    branches: [main]
    paths:
      - 'packages/svg-filter-atlas/**'
      - 'src/filter-atlas.ts'
      - 'src/main.ts'
      - 'package.json'
      - 'package-lock.json'
      - '.github/workflows/filter-atlas.yml'

jobs:
  validate-and-build:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install Seed-Loom dependencies
        # package.json currently contains knowledge-validation dependencies that
        # have not yet been regenerated into package-lock.json. Use a lockfile-
        # preserving install here so Filter Atlas validation can run without
        # rewriting the repository checkout. Restore `npm ci` after the root
        # lockfile is regenerated and verified in a dedicated dependency PR.
        run: npm install --ignore-scripts --no-audit --no-fund --package-lock=false

      - name: Install atlas development dependencies
        run: npm install --prefix packages/svg-filter-atlas --no-audit --no-fund

      - name: Validate and compile atlas
        run: npm run filters:check

      - name: Build Seed-Loom
        run: npm run build
