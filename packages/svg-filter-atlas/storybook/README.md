# Optional Storybook integration

Copy `FilterAtlas.stories.tsx` into an existing React Storybook project. The package itself deliberately does not require Storybook, React DOM, or a bundler to validate and compile recipes.
